import type { OntologyPayload } from '../../api/types';

// Stub — filled in by Phase 4 task #13.
export function FSMScreen({ data: _data }: { data: OntologyPayload }) {
  return (
    <section className="structure">
      <div className="structure-canvas" style={{ padding: 40, color: 'var(--ink-muted)', fontFamily: 'var(--font-mono)' }}>
        <p>FSM detail screen — Phase 4, pending.</p>
      </div>
    </section>
  );
}
